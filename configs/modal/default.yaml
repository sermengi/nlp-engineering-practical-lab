remote:
  provider: modal
  gpu: null
  cpu: 2
  memory_mb: 4096
  timeout_seconds: 900

storage:
  volume_name: nlp-lab-storage
  remote_root: /storage
  experiments_root: /storage/experiments
  checkpoints_root: /storage/checkpoints
  models_root: /storage/models
  hf_cache: /storage/cache/huggingface
  datasets_cache: /storage/cache/datasets
  transformers_cache: /storage/cache/transformers
  torch_cache: /storage/cache/torch
